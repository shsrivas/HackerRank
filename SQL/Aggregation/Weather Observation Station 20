SET @C:= (SELECT COUNT(*) FROM STATION);

SELECT ROUND(SUM(TS.LAT_N),4)
FROM (
SELECT RN, LAT_N FROM
(SELECT ROW_NUMBER() OVER (ORDER BY LAT_N) AS RN, LAT_N
FROM STATION) AS T
WHERE RN = IF(@C%2 = 1, (@C + 1)/2, @C/2 OR @C/2+1)) AS TS;
